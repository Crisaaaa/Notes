{
  "name": "crisas_notes_app",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "start": "npx json-server -s ./frontend/dist ./backend/notesDb.json",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
